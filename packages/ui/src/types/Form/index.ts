/**
 * Type for the icon URL and position.
 */
export interface IconUrl {
  url: string;
  position: "left" | "right";
}

/**
 * Type for the checkmark position.
 */
export type CheckmarkPosition = "outside" | "inside";
