{
  "name": "mms-notification",
  "version": "0.0.1",
  "description": "Notification utils for MMS",
  "main": "src/index.ts",
  "scripts": {
    "build": "vite build",
    "clean": "rm -rf .turbo && rm -rf dist && rm -rf node_modules"
  },
  "devDependencies": {
    "eslint": "^8.51.0",
    "eslint-config-custom": "workspace:*",
    "tsconfig": "workspace:*",
    "vite": "^4.4.11"
  },
  "dependencies": {
    "firebase": "^10.5.0"
  }
}
