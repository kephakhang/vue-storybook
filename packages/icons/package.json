{
  "name": "icons",
  "version": "0.0.0",
  "description": "",
  "main": "src/index.ts",
  "scripts": {
    "clean": "rm -rf node_modules",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@iconify/vue": "^4.1.1",
    "eslint-config-custom": "workspace:*",
    "tsconfig": "workspace:*"
  }
}
